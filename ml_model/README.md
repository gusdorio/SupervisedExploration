### TODO Requirements:

- Adjust params for running the algorithm inside the container enviroment;
- Change data connection method for database connections (Mongo and Mysql);
- Add algorithm for periodic verification of mysql data;